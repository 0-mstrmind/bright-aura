# Scheduler Script for scheduled task in Birght Aura - Street Light Monitoring System

## Note
- Used for auto check and validate data in firebase/firestore DB on condition
- Currently implemented via GitHub Actions
- Need secret keys (environment vairables to get firebase/firestore access).
